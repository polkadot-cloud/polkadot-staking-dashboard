import styled from 'styled-components';

export const OwnershipItemWrapper = styled.div`
  display: flex;
  flex-direction: row;
  width: 100%;
  gap: 1rem;
  align-items: center;
  .owner {
    flex-grow: 1;
    text-align: left;
  }
  .count {
  }
`;
